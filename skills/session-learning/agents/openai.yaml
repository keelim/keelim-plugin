interface:
  display_name: "Session Learning"
  short_description: "Capture and promote session lessons safely"
  default_prompt: "Use $session-learning to review recent session learning candidates and promote durable lessons into the right project document."

policy:
  allow_implicit_invocation: true
